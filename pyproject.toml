[project]
name = "stewie"
version = "0.1.0"
description = "Stewie — A JARVIS-inspired voice-controlled AI assistant for Windows"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
